<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cloud.artik.example.cloud_connector_device"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
	
	<uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="cloud.artik.example.cloud_connector_device.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="cloud.artik.example.cloud_connector_device.DeviceActivity"
            android:label="@string/play_with_device" >
        </activity>
        <activity
            android:name="cloud.artik.example.cloud_connector_device.AuthorizeDeviceActivity"
            android:label="@string/authorize_device" >
        </activity>
        </application>
    
</manifest>
